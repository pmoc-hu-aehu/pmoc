import 'package:flutter/material.dart';
class PreventivaChecklistScreen extends StatelessWidget {
  const PreventivaChecklistScreen({super.key});
  @override
  Widget build(BuildContext context) => Scaffold(appBar: AppBar(title: const Text('PREVENTIVAS')), body: const Center(child: Text('Em breve')));
}
