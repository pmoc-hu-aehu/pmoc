import 'package:flutter/material.dart';
class QualidadeArChecklistScreen extends StatelessWidget {
  const QualidadeArChecklistScreen({super.key});
  @override
  Widget build(BuildContext context) => Scaffold(appBar: AppBar(title: const Text('QUALIDADE AR')), body: const Center(child: Text('Em breve')));
}
