import 'package:flutter/material.dart';
class DutoChecklistScreen extends StatelessWidget {
  const DutoChecklistScreen({super.key});
  @override
  Widget build(BuildContext context) => Scaffold(appBar: AppBar(title: const Text('DUTOS')), body: const Center(child: Text('Em breve')));
}
