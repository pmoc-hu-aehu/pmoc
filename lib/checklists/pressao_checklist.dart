import 'package:flutter/material.dart';
class PressaoChecklistScreen extends StatelessWidget {
  const PressaoChecklistScreen({super.key});
  @override
  Widget build(BuildContext context) => Scaffold(appBar: AppBar(title: const Text('PRESSÃO')), body: const Center(child: Text('Em breve')));
}
