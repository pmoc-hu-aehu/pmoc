import 'package:flutter/material.dart';
class CorretivaChecklistScreen extends StatelessWidget {
  const CorretivaChecklistScreen({super.key});
  @override
  Widget build(BuildContext context) => Scaffold(appBar: AppBar(title: const Text('CORRETIVAS')), body: const Center(child: Text('Em breve')));
}
