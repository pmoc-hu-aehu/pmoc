import 'package:flutter/material.dart';
class FiltroChecklistScreen extends StatelessWidget {
  const FiltroChecklistScreen({super.key});
  @override
  Widget build(BuildContext context) => Scaffold(appBar: AppBar(title: const Text('FILTROS')), body: const Center(child: Text('Em breve')));
}
