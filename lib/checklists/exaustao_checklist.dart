import 'package:flutter/material.dart';
class ExaustaoChecklistScreen extends StatelessWidget {
  const ExaustaoChecklistScreen({super.key});
  @override
  Widget build(BuildContext context) => Scaffold(appBar: AppBar(title: const Text('EXAUSTÃO')), body: const Center(child: Text('Em breve')));
}
