import 'package:flutter/material.dart';
class MovimentacaoChecklistScreen extends StatelessWidget {
  const MovimentacaoChecklistScreen({super.key});
  @override
  Widget build(BuildContext context) => Scaffold(appBar: AppBar(title: const Text('MOVIMENTAÇÃO')), body: const Center(child: Text('Em breve')));
}
